# 状态（RezUp / honestcv）

详细的逐轮记录在 `docs/handoff-context.md`（英文，每轮 R### 一节，含证据、修法、部署版本、生产 QA、限制）；本文件只放当前状态、未完成事项、关键路径与验证命令。

## 当前状态
- 生产：https://cv.zalize.com ，Cloudflare Worker 版本 `aad2b053`（R858），包 `index-CJftaHDx.js` / `Jobs-D7wPKD_v.js`，`/api/health` 200。
- 最新一轮：R858（`docs/plan-r858-dialog-closing-children.md`）——对话框关闭动画（~200 ms）期间不再对着已清空的状态重绘：生产实测确认后第一帧就是不透明度 1.00、`data-state=closed` 的“Stop tracking 0 jobs?”（单条则是 `Stop tracking ""?`，描述句也缺主语），直到卸载。修在共享原语 `src/components/ui/dialog.tsx`：`Dialog` 经 context 下发 `open`，`DialogContent` 记住最后一次打开态渲染的 children，`open===false` 时渲染快照（纯函数 `src/lib/closingChildren.ts`）；37 个 `<Dialog>` 全是受控的，Dashboard 删除/移除文件夹、Builder 副本删除等同类对话框一并受益。原生复测 375 单条/批量全程一个标题。
- 上一轮：R857（`docs/plan-r857-bulk-checkbox-hit.md`）——Tracked 页批量选择的每行复选框从 16×16 裸 input 变为 36×44 的 `<label>` 命中盒（负外边距抵消内边距，行高不变），可见框、`aria-label`、原生语义不变；`Select…`/状态 select/`Untrack N` 本就是 40/32 px。**第二次部署**：首版 label 距卡片按钮 2 px，独立 QA 的 CDP 触摸扫描发现 label 右侧 4 px 内的点击被 Chrome 触摸吸附交给卡片（开面板、URL 带 `job=`、复选框不变；去掉 label 后边界相同，即浏览器吸附非 label 几何）→ 批量态行间距 `gap-2.5`→`gap-4`，卡片右移 6 px、距 label 8 px；原生复测 375 半径 1/12 从复选框中心到 label 右缘每 2 px 全部切换。
- 分支：`devin/1789056470-r858-bulk-dialog-zero`（叠在 R857 分支 `devin/1789053127-r857-bulk-checkbox-hit` 之上；#1078 已合入 R845 分支）。
- 级联 PR #1064（R708–R854 一次落 main）可合并、待老板点 Merge；Devin 侧策略拒绝直接合入 main/master，叠放分支之间的 PR 可自行合并。

## 未完成事项
- 老板侧：合并 #1064；重生成 `LICENSE_SIGNING_SECRET` / `LEMONSQUEEZY_WEBHOOK_SECRET` / `LS_VARIANT_*_ID`；中继 `code-plan.site` 仍无 DNS 记录（仓库外 P0，AI 功能走 `llm:down` 降级）。
- R858 独立生产 QA 已通过（`aad2b053`，375/1280，144 断言 0 缺陷：18 次关闭全程一个标题/描述、首个 closed 帧不透明度 1、卸载 196–276 ms；重开立即换新名；Cancel/Undo/快速 Tab→Dismiss 焦点路径不回归；移动端 axe 0 违规，批量描述 `color-contrast` 为 incomplete 非违规）。证据 `/home/ubuntu/qa/r858-qa/`、PR #1079 评论。未覆盖：真机/读屏、Builder 对话框。
- R859 候选（先取证）：Builder 副本删除/最终下载检查对话框同一关闭帧观察器；粘性栏 `partiallyObscured` 用户可见案例；375 批量态首次勾选 48 px 行下移。
- R857 剩余候选：粘性栏 `partiallyObscured` 类是否有用户可见案例（未量到）；`Dashboard.tsx` 三个删除对话框、`Builder.tsx` 副本删除与最终下载检查对话框的 `onCloseAutoFocus` 是否有 R856 同类竞争（已定位未实测）。
- 375 已知观察（未改）：首次勾选后状态 select/`Untrack N` 换到第二行，所有行下移 48 px 一次。
- 已知 lint 基线：`npm run lint`（`eslint .`）在历史夹具 `.tmp-smoke/r345_oracle.ts` / `r346_oracle.ts` 上报 4 个既有错误（`no-explicit-any` / `no-unused-expressions`），产品范围 `npx eslint src tests worker` 为 0 错误 / 11 既有警告；未改动这些夹具。

## 关键路径
- 前端：`src/pages/Jobs.tsx`（岗位板）、`src/pages/Builder.tsx`、`src/lib/useFocusAfterRender.ts`、`src/lib/importText.ts`。
- Worker：`worker/index.ts`；构建产物 `dist/client/assets/`。
- 测试：`tests/`（vitest，结构测试 `tests/jobs-panes.test.ts`）。
- QA 探针（本机，不入库）：`/home/ubuntu/qa/lib.cjs` + `r###-*.cjs`；测试技能 `.agents/skills/testing-rezup/SKILL.md`。

## 验证命令
```bash
npx vitest run                 # 全部单测
npx tsc -b                     # 类型
npm run lint                   # eslint .（含 .tmp-smoke 历史夹具 4 个既有错误）；产品范围：npx eslint src tests worker（0 错误，11 条既有警告）
npm run build && node scripts/verify-dist.mjs   # 构建 + 静态页/站点地图校验
# 部署（新账号凭证；默认环境的 `wrangler deploy` 会因 account_id 不匹配报鉴权错误）
unset CLOUDFLARE_API_TOKEN CLOUDFLARE_ACCOUNT_ID
CLOUDFLARE_API_KEY=$CLOUDFLARE_NEW_GLOBAL_API_KEY CLOUDFLARE_EMAIL=$CLOUDFLARE_NEW_ACCOUNT_EMAIL npm run deploy
curl -s -o /dev/null -w '%{http_code}\n' https://cv.zalize.com/api/health
```
部署后先在无缓存上下文里读已载入包名（`performance.getEntriesByType('resource')`）再量生产。

## 崩溃恢复
`git status` 确认工作树 → 读本文件与 `docs/handoff-context.md` 末节 → 看最近 PR 描述 → 从中断点继续。
