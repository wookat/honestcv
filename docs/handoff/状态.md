# 状态（RezUp / honestcv）

详细的逐轮记录在 `docs/handoff-context.md`（英文，每轮 R### 一节，含证据、修法、部署版本、生产 QA、限制）；本文件只放当前状态、未完成事项、关键路径与验证命令。

## 当前状态
- 生产：https://cv.zalize.com ，Cloudflare Worker 版本 `6c075e5b`（R855），包 `index-BM1raY7x.js` / `Jobs-CtZAIQE0.js`，`/api/health` 200。
- 最新一轮：R855（`docs/plan-r855-jobs-bulk-undo-deep-link.md`）——Tracked 页批量 untrack 的 Undo 焦点回到被还原的行；冷 `?job=` 深链在单栏布局下与点击同几何（面板顶 63）；普通 Undo 的 +98/+44 px 是 `Hide:` 筛选组卸载/重挂 + 滚动锚定，非缺陷。
- 分支：`devin/1789048601-r855-bulk-undo-focus`（叠在 R845 分支 `devin/…-r845-*` 之上）。
- 级联 PR #1064（R708–R854 一次落 main）可合并、待老板点 Merge；Devin 侧策略拒绝直接合入 main/master，叠放分支之间的 PR 可自行合并。

## 未完成事项
- 老板侧：合并 #1064；重生成 `LICENSE_SIGNING_SECRET` / `LEMONSQUEEZY_WEBHOOK_SECRET` / `LS_VARIANT_*_ID`；中继 `code-plan.site` 仍无 DNS 记录（仓库外 P0，AI 功能走 `llm:down` 降级）。
- R855 未做独立测试机 QA 轮（只有原生探针）；375×500 冷深链未在新包上量。
- 下一轮 R856 候选：375×500 冷 `?job=`；Tracked 页 375 批量操作（Select… 行/复选框命中区）；粘性栏 `partiallyObscured` 类是否有用户可见案例。

## 关键路径
- 前端：`src/pages/Jobs.tsx`（岗位板）、`src/pages/Builder.tsx`、`src/lib/useFocusAfterRender.ts`、`src/lib/importText.ts`。
- Worker：`worker/index.ts`；构建产物 `dist/client/assets/`。
- 测试：`tests/`（vitest，结构测试 `tests/jobs-panes.test.ts`）。
- QA 探针（本机，不入库）：`/home/ubuntu/qa/lib.cjs` + `r###-*.cjs`；测试技能 `.agents/skills/testing-rezup/SKILL.md`。

## 验证命令
```bash
npx vitest run                 # 全部单测
npx tsc -b                     # 类型
npm run lint                   # eslint（0 错误，11 条既有警告）
npm run build && node scripts/verify-dist.mjs   # 构建 + 静态页/站点地图校验
# 部署（新账号凭证）
unset CLOUDFLARE_API_TOKEN CLOUDFLARE_ACCOUNT_ID
CLOUDFLARE_API_KEY=$CLOUDFLARE_NEW_GLOBAL_API_KEY CLOUDFLARE_EMAIL=$CLOUDFLARE_NEW_ACCOUNT_EMAIL npm run deploy
curl -s -o /dev/null -w '%{http_code}\n' https://cv.zalize.com/api/health
```
部署后先在无缓存上下文里读已载入包名（`performance.getEntriesByType('resource')`）再量生产。

## 崩溃恢复
`git status` 确认工作树 → 读本文件与 `docs/handoff-context.md` 末节 → 看最近 PR 描述 → 从中断点继续。
