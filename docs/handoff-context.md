# 交接上下文（Handoff Context）

> 按 company-os/templates/handoff-context.md 维护，每次里程碑后更新。

## 项目目标
RezUp——面向海外求职者的英文简历产品（ATS 友好、诚实简历理念），属「Zalize」求职校招线，保留海外独立品牌（part of Zalize 背书）。当前阶段：上线运营（FREE_MODE 开启，付费待 Lemon Squeezy 全配置）。

## 代码与数据位置
- 仓库：`https://github.com/wookat/honestcv`（默认分支 `main`）
- 本地路径：`/home/ubuntu/repos/honestcv`
- 关键子目录：`src/`（React 19 SPA）、`worker/index.ts`（Hono Worker：API+资产+安全头+IndexNow cron）、`scripts/`（prerender/build-seo）、`docs/`（调研/验收/测试计划）
- 数据存储：Cloudflare KV（binding `KV`，namespace HONESTCV_KV）——licenses / free quotas / leads

## 技术栈
React 19 + Vite + Tailwind + Radix / Hono on Cloudflare Workers（assets run_worker_first）/ KV / 每周一 IndexNow cron / docx+pdf-lib 导出

## 当前实时服务状态
- 线上地址：https://cv.zalize.com（wrangler custom_domain 路由）
- 部署：`npm run deploy`（= tsc -b + vite build + prerender + build-seo + wrangler deploy）
- 本地验证：`npm run lint`、`npm run build`

## 当前数据概况
- KV 存 license/免费额度/邮件 leads；无关系型数据库
- 已消费简历中心 ResumeProfile v1（/share/ 链接导入，PR #199/#200；注意 CSP connect-src 需包含简历中心 API 域）

## Rezi 对标循环（2026-08-29 起，R1–R20 已完成，R16 为调研文档轮）
- 一手审计：docs/audit-2026-08-29-rezi-r1.md（R8 复审计截图 ~/audit-r1/shots-r8/）；各轮方案 docs/plan-r1..r11-*.md
- R1 评分深度+AI keyword bullet（PR #214）、R2 落地页 showcase+导航（#215）、R3 逐行 AI 修复（#216）、R4 多页实时预览（#217）、R5 简历操作台 /dashboard（#218，含 SPA_ROUTES 补 /dashboard 修 404）、R6 可保存 cover letter/interview 文档进 dashboard（#219）、R7 Auto-fit 一键排版（#220，真实 PDF 计页选最可读组合）、R8 dashboard 内 Sample library（#221，搜索+行业筛选，复用 examples.json）、R9 移动端汉堡导航（#222，React header + 静态页零 JS details 菜单）、R10 LinkedIn「Save to PDF」档案导入（#223，extractPdf 双栏检测+LinkedIn 专用解析：公司在前/时长括注/多角色公司/侧栏 Contact·Top Skills）、R11 落地页信任深度（#224，ATS「The problem」教育区块+FAQ 5→10 条+退款保证醒目 badge；不造假社会证明）、R12 P2 清理（#225，/api/za/session 未登录改 200 {email:null} 消除 console 401——探测仅在 Builder Import 弹窗打开时触发；页面 Cache-Control 300/600→60/60 收窄部署后旧 HTML 窗口，CACHE_VER 3→4）、R13 导航下拉信息架构（#226，Resources ▾ 下拉：guides/vs Zety/vs LiveCareer/one-time payment/About，React ResourcesDropdown + 静态页零 JS details.rnav，移动菜单加 Resources 分组）、R14 辞职信生成器（#227，对标 Rezi Resignation Letter Generator：第三种 career 文档 kind 'resignation'，Worker /api/ai/resignation-letter 同额度门控，Builder 弹窗 4 输入不依赖 JD，离线模板路径；QA 修两个 P2：dashboard Open 弹窗副标题三分支 + 工具按钮 375px 36→40px）、R15 字体族选择（#228，对标 Rezi Finish Up 工具栏 Font 下拉：Resume.fontFamily 'auto'|'serif'|'sans' + serifOf()，预览/PDF(Times↔Helvetica 标准字体)/DOCX(Georgia↔Calibri) 三处生效，Builder 设计工具条加 Font 三键；2026-08-29 登录实测 Rezi 编辑器截图 ~/audit-r1/shots-r15/）、R16 Job Search 架构调研决策文档（#229，仅 docs：登录实测 Rezi job-search 双栏职位板+管线标签+Target Resume 打通，截图 ~/audit-r1/shots-r16/；方案 B 推荐：Worker 代理外部职位 API+KV 缓存，管线状态 localStorage，Remotive 免 key MVP；重大方向待老板评审后再实现）、R17 Job Search MVP（#230，按 CHARTER「提议即默认方案」落地方案 B 免 key 版：Worker GET /api/jobs/search 代理 Remotive+KV 缓存 1h，服务端剥 HTML；新路由 /jobs 双栏职位板+状态标签 Saved/Applied/Interviewing/Rejected（localStorage honestcv.jobPipeline）+Target my resume 写入草稿 targetRole/jobDescription 打通 tailoring；导航加 Jobs，SPA_ROUTES 加 /jobs；来源如实标注 Remotive，不伪造匹配分）、R18 Sections spacing + Section divider（#231，对标 Rezi Finish Up 工具栏剩余两项：Resume.sectionSpacing 'tight'|'normal'|'roomy'（SECTION_SPACING 0.6/1/1.4）+ sectionDivider 'auto'|'on'|'off' + sectionSpacingOf()/dividerOf() helper，预览/PDF/DOCX 三处生效；band 模板不受 Divider 影响；QA 中发现预览 band+On 多画 1px 线的 P2 当轮修复并复验）
- R19 职位板深度（#232，对标 Rezi Job Search 顶栏：搜索框从草稿 targetRole 预填并首载即搜、Remotive category 筛选（Worker 校验 slug 并按类别标签后置过滤——2026-08-29 实测上游忽略 category 参数，KV key jobs:v3:<q>|<category>）、地点客户端子串筛选、Relevance/Newest 排序、公司 logo（CSP img-src 加 remotive.com，onError 隐藏）；不做 Matched/匹配分（无诚实模型））
- R20 职位行级快捷操作（#233，对标 Rezi 列表行内 Add to Saved/Change status：每行 Save 切换 + 状态 select（No status 移除条目），与详情面板双向同步；管线标签行显示 updatedAt 相对时间戳如「Saved today」；纯 UI 批次无存储变更）
- R21 操作台工作区侧栏（#234，对标 Rezi app 左侧常驻侧栏：/dashboard 与 /jobs md+ 显示 WorkspaceNav——Create new resume 按钮 + My resumes/Career documents(#documents)/Sample library(#samples)/Job search/ATS checker 链接，活跃项 aria-current + 本地数据计数徽标；<md 隐藏靠汉堡菜单；纯前端无 Worker/存储变更）
- R30 AI 相关技能建议（#243，2026-08-29 登录复审计 ~/audit-r1/shots-r30/：Rezi 编辑器 Skills 区有「AI SKILLS EXPLORER」——输入一个技能即返回相关技能 chips（实测输入 React 得 Angular/Svelte/Vue.js/Ember.js 等）；我方技能 chips 为 skillSuggestionsFor(role) 静态硬编码 role family，未知 role 无建议且忽略已有技能/JD；对应新增 Worker POST /api/ai/skill-suggest（skills/role/JD → ≤12 个相邻 canonical 技能名 JSON 数组，skills+role 均空 400，门控同 rewrite，解析失败 502 不扣额度）+ Builder Skills 区新增「AI suggest related skills」按钮（空 seed 内联报错零调用；成功后 AI chips 替换静态 chips，大小写不敏感去重，点击追加进 Skills 字段）；刻意不复制 Rezi 的类别下拉两步模态（其类别 select 实测只有占位项）与服务端技能持久化；生产 QA 全绿（bundle index-Da4bVSSf.js，恰好 1 credit，402 路径未重测））
- R35 操作台卡片直接下载（2026-08-30 登录复审计 ~/audit-r1/shots-r35/：Rezi resumes 工作区每张简历卡 ⋮ 菜单含 Settings/History/Duplicate/Review/Move/**Download**/Delete——无需进编辑器即可导出任意简历，其编辑器 URL 为 /dashboard/resume/<id>/<section>；我方卡片仅 Open/Duplicate/Rename/Delete，导出已存副本必须先 Open 换掉当前草稿再去 /builder 下载；对应 /dashboard 草稿卡与版本卡新增 PDF/DOCX 按钮，复用 downloadResumePdf/Docx 动态 import 与 Builder 同款门控：unlocked 直下、!freeMode 弹 UpgradeDialog、freeMode 未过 email 门弹 FreeDownloadDialog（过门后继续下载并设 honestcv.shared）；不做 Builder final-check 弹窗（卡片下载为拿已定稿副本）；刻意不做：Move/文件夹、卡片 Settings、付费人工 Review、TXT/MD 卡片导出）
- R36 已存副本卡片级 Retarget 设置（2026-08-30 登录复审计 ~/audit-r1/shots-r36/：Rezi 卡片 ⋮→Settings 弹「Update your resume」——名称/Experience/重导入/Language/**Target your resume（Job title/Company/JD）**，无需打开编辑器即可改一份简历的定向职位；History 子菜单为 Undo/Redo/Versions（R28 已覆盖）、另有列表视图切换（P2 未做）；我方改副本定向必须 Open 换草稿→/builder→存回三步；对应版本卡 Rename 铅笔升级为 Settings 弹窗（Name + Target role + Job description，保存走新 lib/resume.ts updateResumeVersion 刷新 updatedAt，卡片 ATS 分即时按新 JD 重算），零新 API/AI/存储 key；刻意不做：Experience 下拉（无诚实行为支撑）、卡片内重导入（R24 磁贴已覆盖）、Language、列表视图、Move/文件夹）
- R37 操作台副本排序 + 网格/列表视图（2026-08-30 登录复审计 ~/audit-r1/shots-r37/：Rezi resumes 工作区右上有「Sort by: Name/Created/Edited」下拉 + grid/list 视图切换，列表视图为 Name/Created/Edited 行表格 + 行尾同款 ⋯ 菜单；我方 My resumes 仅固定顺序卡片网格；对应网格上方新增控制行——Sort 下拉（Last edited 默认/Name A–Z，无 createdAt 字段故诚实不提供 Created）+ Grid/List 切换（aria-pressed），排序仅作用于 versions（draft 卡+磁贴固定在前），List 视图为磁贴网格下方的行列表（名称/Edited/ATS + 与卡片共用 versionActions(v) 全操作），视图偏好持久化新 key honestcv.dashboardView（'grid'|'list'）、排序不持久化；刻意不做：Move/folders、Review、行内缩略图列）
- R42 助手工作台入口 + Target my job 快捷任务（#255，2026-08-30 登录复审计 ~/audit-r1/shots-r42/：Rezi AI Resume Agent 是一等侧栏目的地 /dashboard/agent（含 recent chats），快捷任务为 IMPROVE MY REZI SCORE/TARGET MY RESUME/FIND JOBS，免费层对话数封顶弹 $29/mo 付费墙（回复后 suggestion chips 无法继续取证）；我方助手此前只能从 /builder 内图标进入且无 job targeting 快捷任务；对应 /builder?assistant=1 深链开面板即清 URL（复用 ?doc= 模式，开面板零 AI 调用）、WorkspaceNav（/dashboard 与 /jobs）新增 AI assistant 项、快捷任务加第 4 条 Target my job（JD 差距分析 prompt）；刻意不做：独立全屏 agent 路由、recent-chats 列表、FIND JOBS 聊天任务（/jobs 一键可达，聊天导航浪费付费 AI 调用）；生产 QA 全绿（bundle index-JGp_30uF.js；披露——/jobs 侧栏项仅截图核验未实点，与 /dashboard 同代码路径已实点））
- R41 助手 Apply 显式确认写操作（#254，落地 R39 决策的第二半：Rezi Agent 能真实写入工作区，R40 刻意只建议不写；现助手在用户明确请求改摘要/加技能时可在回复尾部输出单行 @@APPLY {"type":"summary"|"skills","value":…}，Worker parseAssistantAction 剥离并校验（summary ≤700 字符、skills ≤12 条各 ≤40，畸形尾部静默降级为纯文本不报错）、端点返回 { text, action, freeRemaining }；前端 Apply 卡片（预览 + Apply to summary / Add to skills → Applied 状态，随聊天历史持久化、每卡只能 Apply 一次、Apply 零网络调用零额度），Builder onApply 用既有 setResume 做摘要替换/技能大小写不敏感去重追加，故可走既有 Undo/History 撤销；生产 QA 全绿含对抗性去重与 reload 后 Apply；披露——375px 下 Apply 按钮本身未逐像素量高（同面板控件均 40px + 代码 min-h-10 佐证））
- R40 Resume Assistant MVP（#253，基于 R39 调研决策 docs/plan-r39-ai-agent-research.md：Rezi 有一等 AI 聊天工作台而我方全是按钮式单任务 AI，P1 架构差距；对应 /builder 头部新增 MessagesSquare 按钮打开右侧聊天面板 src/components/AssistantPanel.tsx——桌面 420px、<sm 全宽，空态 3 个快捷任务（Improve my ATS score/Draft my summary/Suggest skills）仅点击才发送，Enter 发送/Shift+Enter 换行，历史存 localStorage honestcv.assistantChat 上限 40 条 + Clear chat；Worker 新端点 POST /api/ai/assistant 与既有 AI 端点同 bundle/免费额度门控、成功才扣额度，校验 turns（末条须 user、保留最近 12 条、每条 ≤2000 字符），buildAssistantMessages 以 resumeToPlainText 草稿（≤6000）+ targetRole + JD（≤4000）为系统上下文并禁止捏造事实、按名指向既有编辑器工具；诚实边界——助手只建议不写简历，显式确认的 Apply 写操作留后续轮；QA 发现 375px 头部工具栏溢出（本按钮加剧既有 444→492px），已当轮修复 bb767ff——<sm 隐藏 Undo 与 DOCX 按钮（DOCX 移动端仍可走 dashboard 卡片），复验 scrollWidth=375、面板真全宽、Close 在屏内；QA 期间遇上游 LLM relay 一段 503 停机，502 透传+不扣额度语义验证正确，恢复后 grounded/多轮/额度全部实测通过）
- R38 操作台文档直建入口（2026-08-30 登录复审计 ~/audit-r1/shots-r38/：Rezi 工作区顶部 RESUMES/COVER LETTERS/RESIGNATION LETTERS 三标签，各自页面有虚线「Create new …」直建磁贴——文档新建是工作台一等入口；我方 Career documents 只列已存文档，新建必须去 /builder 自己找工具；对应标题区下新增三个直建按钮 New cover letter / New interview prep / New resignation letter，Link 到 /builder?doc=cover|interview|resignation 复用 R33 深链，空态与非空态均显示，零新 API/存储/AI；刻意不做 Rezi 式独立标签页——单页 dashboard + R23 类型筛选已覆盖浏览侧）
- R34 落地页产品套件区（2026-08-29 复审计 ~/audit-r1/shots-r34/：Rezi 首页把 Create Resumes/Generate Cover Letters/Search Jobs/Interview Prep 并列为一等入口，且实测其免费层 Create new Cover Letter 直接弹 $29/mo Pro 付费墙；我方落地页只讲简历——cover letters/interview prep/job search 仅在定价表 bullet 出现一次，无任何功能级入口；对应 Landing「What you get」后新增「More than a resume builder」套件区：4 卡（Cover letters→/builder、Interview prep→/builder、Job search→/jobs、Resume dashboard→/dashboard），复用 FEATURES 卡片样式+link CTA，文案如实不造用户量/评价；刻意不做：testimonials、营销视频、导航改动、新路由）
- R33 职位→求职信直达路径（2026-08-29 复审计 ~/audit-r1/shots-r33/：Rezi 首页/定价把「Generate Cover Letters」与 Search Jobs 并列为核心工作流，我方定价页承诺「AI cover letters tailored to each job posting」但 /jobs 职位详情只有「Target my resume」，找到职位后没有任何直达求职信的入口；对应 /jobs 详情区新增「Cover letter」按钮——复用确认弹窗（intent 'target'|'cover'，两种 intent 都把 job.title/description 写入草稿），cover intent 导航到 /builder?doc=cover&company=<encoded>；Builder 以 useState 初始化器读取 ?doc/?company 深链（与既有 ?template/?example 模式一致），mount 时 history.replaceState 清理 URL 防刷新重开，BundleToolDialog 新增可选 initialCompany prop 预填 Company 输入；零新 API/存储/AI 调用——入口本身不消耗额度，仍需用户点 Generate 才调用既有 /api/ai/cover-letter；刻意不做：自动生成（未确认即扣额度）、独立 cover letter 编辑器、云端持久化）
- R32 真正的「新建简历」流程（2026-08-29 复审计 ~/audit-r1/shots-r32/：Rezi「CREATE NEW RESUME」打开引导式 Create a resume 弹窗——RESUME NAME 必填、EXPERIENCE/LANGUAGE 下拉、导入 PDF/DOCX、「Target your resume」建卡即定向到具体职位、免费人工排版审阅 opt-in；我方 WorkspaceNav「Create new resume」只是 Link 到 /builder 打开旧草稿，产品内无任何新建简历入口，开第二份简历有静默丢失当前草稿风险；对应 Dashboard 新增 NewResumeDialog——可选 Target role + Job description（写入 targetRole/jobDescription 使 ATS 关键词匹配从第一笔生效）+ 有草稿时默认勾选「Keep a copy of my current draft」（saveResumeVersion），My resumes 网格新增虚线「Start a new resume」磁贴（覆盖 <md 无侧栏场景），WorkspaceNav 增加可选 onCreate prop（/dashboard 打开弹窗，/jobs 保持原 Link 行为）；刻意不做：必填简历名（存副本时才命名）、经验级别/语言下拉（背后无诚实可实现行为）、人工排版审阅 opt-in（人工服务商业模式）、AI Resume Agent 路径）
- R31 信头信函导出（2026-08-29 复审计 ~/audit-r1/shots-r31/：Rezi 编辑器有专属 AI COVER LETTER 区，信函为按简历管理的真实文档、条目预览带发件人姓名+联系方式（完整渲染 Pro 门控未实测）；我方 cover/resignation letter 走通用 downloadTextPdf/Docx——硬编码 modern 模板+Helvetica+Letter、顶部印「Cover Letter」标题、无姓名/联系方式/日期；对应新增 pdf.ts downloadLetterPdf + docx.ts downloadLetterDocx（发件人姓名+可点击联系行 email/phone/location/website+模板 accent 分隔线+当日日期+正文段落，字体族/纸张尺寸跟随简历 serifOf/pageSize），Builder 的 cover/resignation PDF/DOCX 按钮切换到信函导出，interview brief 保持通用文本导出（私人笔记非信函）；刻意不做：编辑器内按简历分列的 cover-letter 条目（弹窗+career docs 已覆盖工作流）、渲染式信函预览、Pro 门控）
- R29 AI 摘要生成（#242，2026-08-29 登录复审计 ~/audit-r1/shots-r29/：Rezi 编辑器 Summary 区有「AI Summary Writer」——POSITION/SKILLS HIGHLIGHT + from resume 预填，用户无需先写草稿即可生成；我方「AI polish summary」在摘要为空时拒绝服务；对应新增 Worker POST /api/ai/summary-draft（resumeText+role → 严格只用简历已有事实生成 3 条候选摘要 JSON 数组，门控同 rewrite，解析失败 502 不扣额度）+ Builder Summary 区在摘要为空时按钮变「Draft from my resume」（简历无内容则内联报错零调用；成功复用 Pick a summary 变体选择弹窗）；刻意不复制独立 highlight 输入（target role 字段已覆盖）与 AI Skills Explorer）
- R28 编辑器自动编辑历史（#241，2026-08-29 登录复审计 ~/audit-r1/shots-r28/：Rezi 编辑器侧栏有「Version History (beta)」自动快照时间线（相对时间戳条目）；对应新增 localStorage `honestcv.resumeHistory`（{id,at,data} 最多 15 条，仅当与最新快照内容不同且最新快照 ≥10 分钟时记录，restore 前 force 记录当前草稿保证可逆）+ /builder 头部 History 时钟按钮打开「Edit history」弹窗（相对时间 + 姓名—目标角色 + Restore，当前草稿标 Current；空态说明文案）；QA 发现 375px 头部 History/Undo 按钮 32px，已当轮修复为 40px（min-h-10 min-w-10 sm:min-h-8 sm:min-w-8）并复验；同轮复核确认 Finish Up 工具条/Keyword Targeting/逐 bullet 最佳实践检查我方已覆盖，Expert Review 付费人工评审属商业模式差异不复制）
- R27 练习问题 AI 建议（#240，2026-08-29 登录复审计 ~/audit-r1/shots-r27/：Rezi AI Interview 向导「questions will be based on your resume / job title and description will guide the AI interview questions」——用户无需自己想问题；对应新增 Worker POST /api/ai/interview-questions（resumeText+JD+role → 最多 5 条 ≤200 字符问题 JSON 数组，门控同 interview-feedback，解析失败 502 且不扣额度）+ Practice 区「Suggest questions」按钮（无 JD 内联报错零调用，结果为可点按钮填充问题输入并清除旧反馈））
- R26 面试答案练习+AI 反馈（#239，2026-08-29 登录复审计 ~/audit-r1/shots-r26/：Rezi「AI INTERVIEW」为视频面试模拟器、「AI RESUME AGENT」为聊天；诚实降级为文本练习——Interview Prep 弹窗底部新增「Practice an answer」区：问题+答案输入，Worker 新端点 POST /api/ai/interview-feedback（与 interview-brief 同 bundle/免费额度门控，输出 WHAT WORKED/WHAT TO IMPROVE/STRONGER ANSWER，占位符不造经历）；Agent 聊天面属更大架构决策留待后续轮）
- R25 工作区计划/用量面板（#238，对标 Rezi 侧栏「PRO · RESUMES 1/1 · AI GENERATIONS 1/10 · UPGRADE」：WorkspaceNav 底部新增「Your plan」卡——Free/Resume/Bundle plan 名称来自 loadLicense()，免费用户显示只读 GET /api/ai/quota 的「Free AI credits left: N」（取失败隐藏该行）+ Upgrade 链接到 /pricing/，持照用户显示 Unlimited AI 无 CTA；不伪造简历数量上限，无新端点/存储；<md 隐藏不变）
- R24 操作台导入磁贴 + 相对时间戳（#237，2026-08-29 登录复审计 ~/audit-r1/shots-r24/：Rezi resumes 页建简历卡支持「or drop a resume here」且卡片显示「Edited 3 hours ago」；/dashboard My resumes 网格新增虚线「Import a resume」磁贴——点选/拖放 PDF/DOCX/TXT 浏览器内 extractTextFromFile+parseResumeText 解析后写入草稿跳 /builder，已有草稿时弹确认弹窗（可先存副本），无文本/读取失败磁贴内联报错；版本卡片日期改「Edited today/N days ago」相对时间；纯前端无存储/Worker 变更）
- R23 操作台文档类型筛选（#236，对标 Rezi 侧栏 RESUMES/COVER LETTERS/RESIGNATION LETTERS 分列：/dashboard「Career documents」标题下新增类型筛选 pill——All/Cover letters/Interview prep/Resignation letters 带计数，零计数类型不显示，删除某类型最后一份自动回落 All；纯前端客户端过滤，honestcv.careerDocs 存储不变）
- R22 落地页 hero 简历投递区（#235，对标 Rezi 首页「Upload or drop your resume to get started / Score resume」：hero CTA 下方新增虚线拖放/点选上传区，浏览器内 extractTextFromFile 提取后带 router state 跳 /ats-checker 自动出分（空 JD 仍出格式分）；提取失败/无文本内联报错留在落地页；文案明示「never uploaded to a server」；纯前端无 Worker 变更）
- PR 为累积链（R2 基于 R1 分支…R25 基于 R24），生产已部署最新版（R21 部署时 wrangler 上传成功但 zones/routes 同步 API 报 Authentication error 10000——自定义域路由已存在故不影响上线，新 bundle 已实测生效；若后续需改路由需补 Cloudflare token 的 zone Workers Routes 权限）；PR 均未合并，合并时按链顺序
- 剩余候选差距（R8 复审计确认）：Rezi Job Search 已由 R16/R17 落地免 key MVP（后续扩源需 Adzuna/Jooble key，已向老板申报）、付费人工 Review（商业模式差异）、落地页信任（P1-8）R11 已做可诚实部分，真实用户评价/用户量待产品上量后再补；导航下拉（P2-9）R13 已做、部署缓存窗口与 session 401 R12 已修（窗口收窄到 ~1 分钟）
- QA 备注（P2，非本轮引入）：React 页 console 偶发 [Report Only] CSP 违规日志（疑似平台注入，无拦截）
- R7 QA 备注（可选优化，非 P0/P1）：Auto-fit 在已最优页数时仍会把设置升到更可读组合；表单控件 id/name 信息性提示已在 dashboard 修复、builder 其余字段未处理
- R15 审计剩余（Rezi 编辑器 Finish Up 实测）：Share 公开链接（需云端持久化，属架构决策）、How You Compare 百分位（无真实数据不做）、Icons/Profile picture（ATS 反模式，刻意不做）、Sections spacing/Section divider 已由 R18 落地（Indent 影响 ATS 解析且 Rezi 默认关闭，不做）；设计工具条按钮 375px 为 27px（与既有一致，如整条升级再提）
- 生产复验标准：1440+375 双视口、无横向溢出、触摸目标≥40px、console clean、文档路由 200

## 进行中/待办任务（按优先级）
1. 付费通道：Lemon Squeezy 配置完成后关 FREE_MODE，回归 checkout 全流程
2. 品牌：遵循 @zalize/brand 版本化包（治理批次 GV-2）
3. 「上岸承诺」条使用英文版且不承诺 free（有付费下载路径）——见 PR #201

## 已知坑与注意事项
- wrangler.jsonc `run_worker_first: true`：所有静态资产也走 Worker（安全/缓存头统一），改 Worker 需回归静态资源
- CSP 在 worker 下发，新增外部 API 域必须同步 connect-src（曾拦截简历中心导入，#200 修复）
- 预渲染在 build 内（scripts/prerender.mjs），SEO 页由 build-seo.mjs 生成，跳过会丢 SEO
- AI 功能走 LLM relay，欠费/不可用时诚实降级，不得伪造 AI 输出

## 资源与凭证索引
- wrangler secrets（名称）：LLM_RELAY_BASE_URL / LLM_RELAY_API_KEY / LEMONSQUEEZY_API_KEY / LEMONSQUEEZY_WEBHOOK_SECRET / LS_STORE_ID / LS_VARIANT_*_ID / LICENSE_SIGNING_SECRET
- vars：LLM_MODEL=glm-5.2、CHECKOUT_ENABLED、FREE_MODE
- Cloudflare 账号：wookat520（Workers 部署）

## Zalize 统一账号接入（UA 项目收口 2026-08-27）

- UA-P2 一键导入主简历（PR #210 已合并）：Worker /api/za/session|primary 服务端代理中心会话与 resume 主简历出口（no-store，不暴露中心凭证）；Builder 导入弹窗登录态显示「导入我的主简历」。简历数据仍只存 localStorage，不做云备份；shareId/文件/文本旧导入路径不变。
- 中心站 account.zalize.com（get-session / token / jwks，Cookie Domain=.zalize.com）；30 天双轨到期日 2026-09-26（到期前旧登录/旧 token 持续有效）；总方案与全阶段 PR 索引见 company-os projects/unified-account/plan.md。
